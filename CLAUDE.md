# moa

Backlog workflow: before working on tickets, read `backlog/!README.md`.

Subagents: launch them in the background (`run_in_background`) by default, so the
user can keep talking while the agent works instead of queuing behind it.
